[package]
name = "tcheran"
version = "0.0.0"
rust-version = { workspace = true }
edition = { workspace = true }

[dependencies]
bullet_lib = { path = "../bullet_lib", features = ["cpu"] }
viriformat = { workspace = true }
